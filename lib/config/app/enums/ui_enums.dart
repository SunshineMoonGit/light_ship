// UI 관련 enum
enum SizeType { small, medium, large }

enum GetImageMethodType { gallery, camera }

enum ImageType { none, profile, card }

enum SettingType { main, auth, profile, custom, display, language }

enum InputType { string, bool, select }

enum ModalType {
  none,
  editProfile,
  editCard,

  profileMore,
}
